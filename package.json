{
  "name": "mandiq-platform",
  "version": "0.1.0",
  "private": true,
  "description": "MandiQ - Intelligent APMC Yard & Dynamic Queue Management Platform",
  "scripts": {
    "build": "cd frontend && npm install && npm run build"
  },
  "devDependencies": {
    "supabase": "^2.117.0"
  }
}
